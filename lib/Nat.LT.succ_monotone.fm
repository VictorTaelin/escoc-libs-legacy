Nat.LT.succ_monotone
: {a : (Nat a)} {b : (Nat b)} {p : (Nat.LT a b p)} (Nat.LT (Nat.succ a) (Nat.succ b) (Nat.LT.succ_monotone a b p))
= [a] [b] [p]
  let x (Nat.LT.fst a b p)
  let e (Nat.LT.snd a b p)
  (Nat.LT.cons (Nat.succ a) (Nat.succ b) x (Eq.cong Nat Nat (Nat.add a x) b e Nat.succ))
